已提交并推送 GitHub。

```text
0731a7c feat: add request ID middleware
```

`go test ./...` 已通过。

注意：两个 SQL 迁移文件目前仍为空，不能算数据库初始化完成；先由基础设施同学负责补齐。

## 四人详细分工

### 1. 成员 A：注册中心与 PostgreSQL

负责“Server / Tool 数据从 API 保存到 PostgreSQL”。

负责目录：

```text
config/
repository/
service/
handler/
db/migrations/
```

具体任务：

1. 补齐并验证 `001_create_mcp_servers.sql`
2. 编写 `002_create_mcp_tools.sql`
3. 增加 `.env.example`
4. 在 `config/config.go` 增加 PostgreSQL DSN 配置
5. 使用 `pgxpool` 创建 PostgreSQL 连接池
6. 编写 `PostgresServerRepository`
7. 让现有接口从内存 Repository 切换到 PostgreSQL：

```text
POST /api/servers
GET  /api/servers
GET  /api/servers/:id
```

8. 实现 Tool 基础接口：

```text
POST /api/tools
GET  /api/tools
GET  /api/tools/:id
POST /api/tools/:id/publish
POST /api/tools/:id/offline
```

验收：

```text
注册 Server 后，重启网关与 PostgreSQL 容器，仍能查询到数据。
同一个 name 或 endpoint 重复注册，返回 409。
同一 Server 下的同名 Tool 不能重复。
Tool 可以在 draft / published / offline 状态切换。
```

交付文件建议：

```text
db/migrations/001_create_mcp_servers.sql
db/migrations/002_create_mcp_tools.sql
config/database.go
repository/postgres_server.go
repository/postgres_tool.go
service/tool.go
service/tool_register.go
service/tool_query.go
handler/tool.go
```

---

### 2. 成员 B：网关与 MCP 调用代理

负责“Agent 如何发现并调用工具”。

负责目录：

```text
client/
service/
handler/
router/
```

具体任务：

1. 设计下游 HTTP Client：

```text
client/mcp_client.go
```

2. 实现网关工具发现：

```text
GET /mcp/tools
```

规则：

```text
只返回 published = true
且所属 Server health_status = online
且 Server status = active
```

3. 实现网关工具调用：

```text
POST /mcp/tools/:toolName/call
```

调用流程：

```text
解析 toolName
→ 查 Tool
→ 查 Server
→ 检查 Tool 是否 published
→ 检查 Server 是否 active 且 online
→ 转发 JSON 请求到 endpoint
→ 设置超时
→ 标准化返回结果
```

4. 定义错误码：

```text
TOOL_NOT_FOUND
TOOL_OFFLINE
SERVER_UNAVAILABLE
UPSTREAM_TIMEOUT
UPSTREAM_ERROR
```

5. 为请求设置超时，例如 5 秒。

验收：

```text
GET /mcp/tools 只出现可调用工具。
不存在工具返回 TOOL_NOT_FOUND。
下线工具返回 TOOL_OFFLINE。
离线 Server 返回 SERVER_UNAVAILABLE。
query_sales 调用能透传示例服务结果。
```

依赖：成员 A 提供 `ToolRepository` 和 `ServerRepository`。

---

### 3. 成员 C：健康检查、示例 MCP Server、Docker

负责“可运行的下游服务和基础部署环境”。

负责目录：

```text
examples/demo-service/
deploy/
db/
```

具体任务：

1. 编写示例服务：

```text
GET  /health
POST /tools/query_sales/call
```

`query_sales` 可先返回固定脱敏销售数据，例如：

```json
{
  "month": "2026-08",
  "total_sales": 128000,
  "region": "华东"
}
```

2. 实现 Server 健康检查 Client：

```text
GET {endpoint}/health
```

3. 实现接口：

```text
POST /api/servers/:id/health-check
```

4. 计算并保存：

```text
health_status
last_health_check_at
latency_ms（可以先只在响应中返回）
```

5. 扩展 Compose：

```text
postgres
demo-service
gateway（最后接入）
```

6. 创建演示初始化数据或 `seed.sql`：

```text
demo-db-server
query_sales
```

验收：

```text
docker compose up -d
→ PostgreSQL healthy
→ demo-service healthy
→ demo-service /health 返回成功
→ 手动健康检查后，Server 状态更新为 online
```

依赖：成员 A 的 Server 持久化与状态更新接口。

---

### 4. 成员 D：测试、接口规范、集成与文档

负责“确保四部分能够稳定合并和演示”。

负责目录：

```text
does/
README.md
tests/（建议新建）
```

具体任务：

1. 整理并维护 API 文档。
2. 定义所有接口请求/响应示例。
3. 统一错误码和状态码。
4. 编写接口测试：

```text
health
server register
server list
server detail
tool register
tool publish/offline
server health-check
mcp tools discovery
mcp tool call
```

5. 编写联调脚本，PowerShell 或 Postman Collection 均可。
6. 更新 README：

```text
环境要求
.env 配置
docker compose 启动
数据库初始化
启动网关
接口示例
完整演示步骤
```

7. 集成验收时执行：

```text
go test ./...
docker compose up -d
接口回归测试
完整调用链测试
```

验收：

```text
一个新成员按 README 能在 15 分钟内启动项目。
所有关键接口都有成功、参数错误、资源不存在场景。
最小演示链路可复现。
```

---

## 推荐协作顺序

```text
成员 A：表结构、PostgreSQL Repository
        ↓
成员 C：示例服务、健康检查、Compose
        ↓
成员 B：工具发现、调用转发
        ↓
成员 D：持续接口测试、最终集成
```

每个人单独创建分支，例如：

```text
codex/registry-postgres
codex/gateway-proxy
codex/demo-health
codex/docs-tests
```

合并前要求：

```powershell
gofmt -w .
go test ./...
git status
```

第一周最小验收链路：

```text
docker compose up -d
→ 注册 demo Server
→ 注册 query_sales
→ 健康检查 online
→ 发布 query_sales
→ GET /mcp/tools
→ POST /mcp/tools/query_sales/call
→ 得到销售数据
```